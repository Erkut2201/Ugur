# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000

# ── Firmendaten (werden in PDFs und E-Mails verwendet — KEIN hardcoded Text) ──
COMPANY_NAME=Konzept Terrasse BW
COMPANY_STREET=Bertholdweg 9
COMPANY_ZIP=72768
COMPANY_CITY=Reutlingen
COMPANY_PHONE=070718826970
COMPANY_EMAIL=acun@konzept-terrasse.de
COMPANY_WEBSITE=www.konzept-terrasse.de

# ── Bankverbindung (für Rechnungs-PDFs) ───────────────────────────────────────
COMPANY_BANK_NAME=HypoVereinsbank
COMPANY_IBAN=DE80700202700045651460
COMPANY_BIC=HYVEDEMMXXX

# ── Steuer (reguläres Unternehmen — MwSt. wird berechnet und ausgewiesen) ─────
COMPANY_TAX_ID=78161/20023
COMPANY_VAT_ID=DE449516879
COMPANY_FINANZAMT=Finanzamt Reutlingen
DEFAULT_VAT_RATE=19

# ── Ansprechpartner / Lieferzeit ──────────────────────────────────────────────
COMPANY_CONTACT_PERSON=Ugur Acun
DEFAULT_DELIVERY_TIME=ca. 6-8 Wochen

# ── Dokumenten-Nummernpräfixe ─────────────────────────────────────────────────
QUOTE_PREFIX=ANG
INVOICE_PREFIX=RNG
PROTOCOL_PREFIX=ABN

# ── Admin-Account (wird beim ersten Start automatisch angelegt) ───────────────
ADMIN_EMAIL=acun@konzept-terrasse.de
ADMIN_PASSWORD=

# ── Session ───────────────────────────────────────────────────────────────────
# Generieren: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
SESSION_SECRET=

# ── PostgreSQL (Produktion — Render/Neon) ─────────────────────────────────────
# Wenn PG_HOST nicht gesetzt ist, wird SQLite als Fallback verwendet
PG_HOST=
PG_PORT=5432
PG_USER=
PG_PASSWORD=
PG_DATABASE=konzept_terrasse

# ── SQLite-Fallback (lokal ohne Postgres) ─────────────────────────────────────
# Nur aktiv wenn PG_HOST leer ist
SQLITE_PATH=./data/local.db

# ── E-Mail (Brevo) ────────────────────────────────────────────────────────────
BREVO_API_KEY=
BREVO_FROM=acun@konzept-terrasse.de
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=acun@konzept-terrasse.de

# ── S3 Object Storage (Produktion) ────────────────────────────────────────────
# Wenn S3_BUCKET nicht gesetzt ist, werden PDFs lokal gespeichert (./data/pdfs/)
S3_BUCKET=
S3_REGION=eu-central-1
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=

# ── PDF-Bilder ────────────────────────────────────────────────────────────────
COMPANY_HEADER_IMAGE=attachments/header.jpg

# ── Lokaler Dateispeicher (Fallback wenn kein S3) ─────────────────────────────
LOCAL_STORAGE_PATH=./data/pdfs
